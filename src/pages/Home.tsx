import React from 'react';
import { styled } from 'styled-components';

const Wrapper = styled.section`
	width: 100vw;
`;

const Home = () => {
	return (
		<Wrapper>
			<h1>HOME</h1>
		</Wrapper>
	);
};

export default Home;
